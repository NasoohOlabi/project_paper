\section{Implementation}
\label{sec:implementation}

The practical implementation follows the same stage order as the workflow runner: data load, research, angle generation, stego synthesis, and decode. Each stage persists artifacts to disk so later stages can reuse the same corpus and hashes. Reproducibility is conditional on identical inputs, model/backend configuration, search responses or cached artifacts, and decoder settings; synthesis and semantic decoding remain configurable rather than universally deterministic.

\subsection{Architectural Components}
The system is operationalized through a series of deterministic pipeline stages. Core components include:
\begin{itemize}
    \item \textbf{Data load stage:} Fetches unresolved URL content and replaces the post body with the extracted text.
    \item \textbf{Research stage:} Generates search terms from the post title, text, and URL using a deterministic LLM configuration (e.g., \texttt{temperature=0}), queries a fixed-shape search backend, removes duplicate links and PDFs, and fetches page text in bounded batches. The stage persists a flat search-results corpus together with reproducibility hashes.
    \item \textbf{Angle generation stage:} Flattens the corpus assembled from the post text, search-results corpus, and comments, then calls the angle runner to produce triplets consisting of a category, a source quote, and a tangent. Long inputs are split into 30k-character segments and batched up to 150k characters. If the backend path fails, the pipeline falls back to a direct LLM prompt that uses the same JSON schema.
    \item \textbf{Stego and decode stages:} Combine the compression layer, comment targeting, semantic shortlist, and verification loop. The encoder generates a small fixed number of candidate comments for the selected Angle, decodes them immediately, and retries synthesis if the selected Angle is not recoverable.
\end{itemize}

\subsection{Codec Module and Sender--Receiver Contract}
The bit layer described in Sections~\ref{sec:compression}--\ref{sec:multiframe} is implemented once, as a module of pure functions shared by both pipelines. Dictionary construction, the field-width function $W$, the token grammar, and the physical-versus-recoverable width rules each have exactly one definition in that module: the sender and receiver pipelines call into it rather than reimplementing any of it, so encoding and decoding stay exact inverses given the same dictionary $D$. This is also what makes the contract testable offline: round-trip tests can drive the codec directly with fixtures, without a model or a network in the loop. It is why the pipelines themselves are restricted to I/O, caching, and orchestration.

Three configuration choices change the bit layer, so sender and receiver must agree on all of them: which payload transform is used (\texttt{plain}, \texttt{hmac\_xor\_v1}, or \texttt{secure\_compact\_v2}), the pre-shared secret required for \texttt{hmac\_xor\_v1} and \texttt{secure\_compact\_v2}, and whether the dictionary capacity profile is applied. The sender records the selected transform in the run audit, so the receiver reproduces it rather than having to infer it. The capacity profile is off by default because dictionary indices are positional: turning it on for only one side would leave the compressor and decompressor referring to different blocks under the same index.
Figure~\ref{fig:impl-pipelines} aligns these components with the staged implementation and on-disk artifacts.
%
% ------------------------------------------------------------------
% AI IMAGE PROMPT (fig:impl-pipelines) -> figures/implementation_pipelines.(png|pdf)
% ------------------------------------------------------------------
% STYLE: Professional academic pipeline diagram, flat vector artwork, white background,
%   restrained but distinct color accents per stage.
% LAYOUT: One horizontal row of five evenly spaced stages connected by thin arrows.
%   Use one short label per stage only; do not repeat labels above and below the icon.
% STAGE 1 LABEL: "Data Load". Visual: URL/document import into a clean data stack or database.
% STAGE 2 LABEL: "Research". Visual: magnifying glass over stacked documents and search results.
% STAGE 3 LABEL: "Angle Generation". Visual: branching tangent paths or clustered notes
%   emerging from a document graph; emphasize thematic options.
% STAGE 4 LABEL: "Stego Encoding". Visual: a message bubble or document with embedded bits,
%   compression symbol, and reply arrow.
% STAGE 5 LABEL: "Decode". Visual: filter/search funnel extracting a key or checkmark from a bitstream.
% VISUAL TONE: Minimal text, no mixed-case identifiers, no underscore-separated identifiers, no duplicate captions, no bullets,
%   no dense annotation.
% COLORS: Use coordinated but muted academic colors per stage (blue, amber, violet, rose, green).
% NEGATIVE PROMPT: repeated labels, code-style names, "GenAngles", angle terminology,
%   decorative marketing style, 3D rendering, neon glow, cluttered background.
% ------------------------------------------------------------------

\begin{figure*}[t]
	\centering
	\includegraphics[width=\textwidth,height=0.55\textheight,keepaspectratio]{figures/implementation_pipelines}
	\Description{Block diagram of implementation pipeline stages and on-disk artifacts supporting deterministic replay and sender--receiver alignment.}
	\caption{Implementation stages and persisted artifacts align with the methodology: each stage is deterministic and replayable where required for sender--receiver parity.}
	\label{fig:impl-pipelines}
\end{figure*}

\subsection{Model Configuration}
The implementation uses different model roles for different stages, but the exact model IDs are treated as configurable parameters:
\begin{itemize}
    \item \textbf{Deterministic stages (term/angle generation):} Run at \texttt{temperature=0} to ensure reproducible reconstruction from the same inputs.
    \item \textbf{Stego synthesis:} Runs at non-zero temperature to generate diverse candidate replies for a fixed embedding choice.
    \item \textbf{Semantic matching and decode:} Uses a semantic similarity model for shortlist ranking, followed by a deterministic discriminative identification prompt (e.g., \texttt{temperature=0}) to select the final Angle index.
\end{itemize}
